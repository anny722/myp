<div class="container">
  <div class="row">
    <% if user_signed_in? %>
      <%= link_to 'Books', books_path, class: 'btn btn-primary' %>
      <%= link_to 'History', histories_path, class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
